#include <stdio.h>
#include <stdlib.h>
/**
 * main - Base 10 numbers
 * Return: 0 will be returned
 */
int main(void)
{
int dee;
 
for (dee = 0; dee <= 10; dee++)

printf("%d", dee);

printf("\n");

return (0);
}
