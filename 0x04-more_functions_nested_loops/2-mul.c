#include "main.h"
/**
 * mul - Entry points for multiplication of numbers
 * @a: the first int to multiply
 * @b: the second int to multiply
 * Return: the result
 */
int mul(int a, int b)
{
int result;
result = a * b;

return (result);


}
