#include "main.h"
/**
 * print_numbers - Entry point of function print number
 * Return: the return value is (0)
 */
void print_numbers(void)
{
int w;
for (w = 0; w <= 9; w++)
_putchar('0' + w);
_putchar('\n');
return;
}
